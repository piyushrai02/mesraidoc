---
title: "Updating"
description: "Update your Mesrai installation."
icon: "arrow-up"
---

import { Card, CardGroup, Steps, Step, Tabs, Tab, Accordion, AccordionGroup, Note, Info, Tip, Warning, Check, Frame, Snippet, Icon, Callout } from '@/components/mdx'

<Note>
  Versions shipping anonymous self-hosted telemetry are enabled by default
  and send one daily heartbeat to `telemetry.mesrai.com` with aggregated
  counters only — no code, names, or identifiers. Inspect what would be
  sent with `yarn telemetry:preview`, or set `MESRAI_TELEMETRY_DISABLED=true`
  to opt out. Full schema and policy in
  [Anonymous Telemetry](/operations/deploy_mesrai/telemetry).
</Note>

To update your Mesrai installation to the latest version:

```bash
# Navigate to your Mesrai installation directory
cd mesrai-installer

# Pull the latest changes from the repository
git pull

# If you are upgrading from 1.x, update your .env with the new 2.0 variables
# (RabbitMQ, MCP Manager, and webhooks).

# Re-run the installer to pull images, start services, and run migrations
./scripts/install.sh

# Optional: run diagnostics
./scripts/doctor.sh
```


