// Operations content is preserved on disk but hidden from public nav.
// All entries hidden — pages still resolve at /operations/* URLs for redirects.
export default {
  mesrai_architecture: { display: 'hidden' },
  deploy_mesrai: { display: 'hidden' },
  local_quickstart: { display: 'hidden' },
  platforms: { display: 'hidden' }
}
